$('#new_prescription_modal').remove();
$('#edit_prescription_modal').remove();
$('body').append('<div class="ui modal" id="new_prescription_modal"><i class="close icon"></i><%= j(render 'new') %></div>');
$('#new_prescription_modal').modal('setting', {autofocus: false}).modal('show');
$('select.dropdown').dropdown({placeholder: false});
$("input").focus(function(){
  $(this).parent().removeClass('error');
  $(this).parent().find('.red.pointing').remove();
});